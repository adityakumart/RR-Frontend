import { AadharFormatDirective } from './aadhar-format.directive';

describe('AadharFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new AadharFormatDirective();
    expect(directive).toBeTruthy();
  });
});
